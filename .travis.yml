language: java

jdk:
  - openjdk7
  - oraclejdk8
  - oraclejdk9

script: mvn clean verify

sudo: false

addons:
  apt:
    packages:
      - oracle-java8-installer

jdk:  
   - oraclejdk8
   - openjdk8
